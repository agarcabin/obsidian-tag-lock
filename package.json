{
	"name": "tag-lock",
	"version": "1.0.1",
	"private": true,
	"description": "Obsidian plugin for password-gating notes by tag or folder.",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^16.11.6",
		"esbuild": "^0.17.3",
		"obsidian": "^1.13.1",
		"typescript": "^4.7.4"
	}
}
